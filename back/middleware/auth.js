/**
 * Authentication Middleware
 * Placeholder for future JWT/Supabase Auth integration
 */

const authMiddleware = (req, res, next) => {
    // TODO: Implement JWT verification
    next();
};

module.exports = authMiddleware;
